'use strict';

var expect = require('chai').expect;
var program = require('../lib/');

describe('sometest', function() {
  it('should find a header', function(done){
    process.nextTick(function(){
      expect(bitmapData.size).to.equal(11078);
      done()
    });
  });
});
